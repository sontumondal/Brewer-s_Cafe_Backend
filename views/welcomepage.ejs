<!DOCTYPE html>
<html lang="en">

<head>
  <title><%= title %></title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Include FontAwesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="dashboard.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
    }

    .main_splash_screen {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      flex-direction: column;
      position: relative;
      text-align: center;
      z-index: 1;
      overflow: hidden;
    }

    .main_splash_screen::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url(/images/main-bg-02.png);
      background-repeat: no-repeat;
      background-position: center;
      background-size: cover;
      opacity: 20%;
      filter: blur(6px);
    }

    .main_splash_screen .main_logo {
      display: inline-block;
      width: 150px;
      height: 150px;
      border-radius: 100%;
      overflow: hidden;
      margin-bottom: 30px;
    }

    .main_splash_screen .main_logo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .main_splash_screen h3 {
      font-size: 30px;
      font-weight: 600;
      position: relative;
      z-index: 1;
    }

    .main_splash_screen h1 {
      font-size: calc(40px + 2vh);
      font-weight: 700;
      position: relative;
      z-index: 1;
    }

    .main_splash_screen .main_wrapper {
      background-color: rgb(255 255 255 / 23%);
      border: 1px solid #d9d9d9;
      padding: 40px;
      border-radius: 40px;
      position: relative;
      z-index: 2;
      backdrop-filter: blur(5px);
      box-shadow: 0 10px 20px #a1a1a161;
    }

    .main_splash_screen .main_wrapper::after {
      content: "";
      position: absolute;
      width: 250px;
      right: -50px;
      background-image: url(/images/leafe.png);
      height: 100%;
      top: 0;
      background-position: center;
      background-repeat: no-repeat;
      background-size: contain;
      filter: blur(2px);
    }

    .food_item_bg {
      position: absolute;
      left: 20px;
      bottom: 20px;
      max-width: 300px;
      width: 100%;
      z-index: 1;
    }

    .spalsh_btn {
      background-color: #000;
      color: #fff;
      font-size: 18px;
      font-weight: 500;
      display: inline-block;
      padding: 12px 25px;
      border-radius: 14px;
      text-decoration: none;
      position: relative;
      z-index: 1;
      margin-top: 20px;
      transition: 0.3s;
    }

    .spalsh_btn:hover {
      background-color: green;
      color: #fff;
    }

    .inner_wrapper {
      position: relative;
      z-index: 1;
    }

    @media screen and (max-width: 600px) {
      .main_splash_screen .main_wrapper{
        margin: 0 20px;
      }
    }
  </style>
</head>

<body>

  <div class="main_splash_screen">
    <div class="main_wrapper">
      <div class="inner_wrapper">
        <span class="main_logo">
          <img src="/images/logo.jpg" alt="">
        </span>
        <h3>Welcome to</h3>
        <h1>Brewer 85 Cafe</h1>
        <a href="/login" class="spalsh_btn">Get Started</a>
      </div>
    </div>
    <img src="/images/food-items-2.png" alt="" class="food_item_bg">
  </div>

</body>

</html>