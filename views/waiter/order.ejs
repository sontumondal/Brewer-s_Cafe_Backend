<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">



    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            /* display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh; */
        }

        .product-card {
            background-color: rgba(255, 255, 255, 0.9);
            /* White with slight transparency */
            color: #333;
            /* Use dark color for the text */
            max-width: 320px;
            width: 100%;
            margin: auto;
            border-radius: 15px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            /* Light gray border */
            overflow: hidden;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
            /* Blurred background for a sleek effect */
            transition: transform 0.3s ease;
            height: 100%;
        }

        .product-card:hover {
            transform: translateY(-10px);
        }

        .product-image {
            position: relative;
            max-height: 200px;
            height: 100%;
        }

        .product-image img {
            width: 100%;
            height: auto;
            border-bottom: 5px solid #4caf50;
            height: 100%;
            object-fit: cover;
            height: 200px;
        }

        .product-info {
            padding: 20px;
        }

        .product-name {
            font-size: 1.5em;
            color: #333;
            /* Black for product name */
            display: flex;
            align-items: center;
            height: 28px;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 1;
            line-clamp: 1;
            text-overflow: ellipsis;
            
        }

        .product-name i {
            margin-right: 10px;
            color: #4caf50;
            /* Green for icons */
        }

        /* Limit description to 2 lines */
        .product-description {
            margin: 10px 0;
            font-size: 0.9em;
            display: flex;
            align-items: center;
            line-height: 1.4;
            color: #333;
            /* Black for description */
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            /* Limit to 2 lines */
            -webkit-box-orient: vertical;
        }

        .product-description i {
            margin-right: 8px;
            color: #4caf50;
            /* Green for icons */
        }

        .read-more a {
            color: #4caf50;
            /* Green for links */
            text-decoration: none;
            font-weight: bold;
        }

        .read-more a:hover {
            text-decoration: underline;
        }

        /* Ingredients section with flex and Read More */
        .product-ingredients {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            margin: 15px 0;
            /* max-height: 60px; */
            /* Limit height to show only 4 ingredients */
            overflow: hidden;
        }

        .ingredient-item {
            display: flex;
            align-items: center;
            background-color: #f4f4f4;
            /* Light background for ingredients */
            color: #333;
            /* Black for ingredients text */
            padding: 5px 10px;
            border-radius: 5px;
            font-size: 0.8em;
            flex-basis: 48%;
            box-shadow: 0 1px 2px rgb(0 0 0 / 14%);
            flex-grow: 1;
        }

        .ingredient-item a {
            text-decoration: none;
            color: #333;
        }

        .ingredient-item a:hover {
            color: #388e3c;
        }

        .ingredient-item i {
            color: #4caf50;
            /* Green for icons */
            margin-right: 5px;
        }

        .price-section {
            margin: 20px 0;
            display: flex;
            justify-content: space-between;
        }

        .price {
            font-size: 1.4em;
            color: #333;
            /* Black for price */
            display: flex;
            align-items: center;
        }

        .price i {
            margin-right: 5px;
            color: #4caf50;
            /* Green for price icon */
        }

        .discount {
            color: #ff5722;
            font-weight: bold;
            display: flex;
            align-items: center;
        }

        .discount i {
            margin-right: 5px;
        }

        .action-buttons {
            /* margin-top: 15px; */
            display: flex;
            justify-content: end;
            gap: 10px;
            position: absolute;
            top: 20px;
            right: 20px;
            width: 95%;
        }

        .action-buttons button {
            background-color: #4caf50;
            border: none;
            padding: 10px;
            color: white;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
        }

        .action-buttons button i {
            /* margin-right: 8px; */
        }

        .action-buttons button:hover {
            background-color: #388e3c;
        }

        .delete-btn {
            background-color: #f44336;
        }

        .delete-btn:hover {
            background-color: #d32f2f;
        }
        .main_product_quick{
            margin: 20px;
        }
        .left_arrow_unic{
            font-size: 25px;
            color: #000;
            margin-bottom: 15px;
            display: inline-block;
            transition: 0.3s;
        }
        .left_arrow_unic:hover{
            color: #d32f2f;
        }
        .main_logo_order{
            width: 60px;
            border-radius: 100%;
        }
        .arrow_logo_wrapper{
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0px 15px;
        }
        .hide_btn{
            margin-top: 18px;
        }
        .hide_btn button{
            background-color: #388e3c;
            color: #fff;
            padding: 6px 15px;
            border: 0;
            border-radius: 6px;
            transition: 0.3s;
        }
        .hide_btn button:hover{
            background-color: #000;
        }
    </style>
</head>

<body>


    <main class="main_product_quick">
        <div class="arrow_logo_wrapper">
            <a href="#" class="left_arrow_unic"><i class="fa-solid fa-arrow-left"></i></a>
            <span><img src="/images/logo.jpg" alt="" class="main_logo_order"></span>
        </div>
        <div class="main_card_product_side">
            <div class="container-fluid">
                <div class="row">
        
                    <% orders.forEach(order=> { %>
        
        
        
                        <div class="col-xl-3 col-sm-6 my-3">
                            <div class="product-card">
        
                                <div class="product-image">
                                    <img src="<%= order.menuItems[0].menuId.image[0] %>" alt="Product Image">
                                    <div class="action-buttons">
                                        <!-- <button class="edit-btn"><i class="fas fa-edit"></i></button>
                                        <button class="update-btn"><i class="fas fa-sync"></i></button>
                                        <button class="delete-btn"><i class="fas fa-trash-alt"></i></button> -->
                                    </div>
                                </div>
                                <div class="product-info">
                                    <h2 class="product-name"><i class="fas fa-box-open"></i>
                                        <%= order.menuItems[0].menuId.name %>
                                    </h2>
                                    <!-- Product Description with Read More link -->
                                    <p class="product-description">
                                        <i class="fas fa-info-circle"></i>
                                        <%= order.menuItems[0].menuId.description %>
                                            <span class="read-more"><a href="read-more-description.html">Read
                                                    More</a></span>
                                    </p>
                                    <!-- Product Ingredients with Read More link -->
                                    <div class="product-ingredients">
                                        <div class="ingredient-item"><i class="fas fa-leaf"></i>Table : <%=
                                                order.table.tableNumber %>
                                        </div>
        
                                        <div class="ingredient-item"><i class="fas fa-leaf"></i>Status : <%= order.status %>
                                        </div>
                                        <div class="ingredient-item"><i class="fas fa-leaf"></i><a
                                                href="/waiter/orders/<%= order._id %>">View Details</a></div>
                                    </div>
                                    <div class="ingredient-item price"><i class="fas fa-leaf"></i>AED <%=
                                            order.totalPrice.toFixed(2) %>
                                    </div>
                                    <div class="hide_btn">
                                        <% if (order.status==='done' ) { %>
                                            <form action="/waiter/orders/serve" method="POST" style="display:inline;">
                                                <input type="hidden" name="orderId" value="<%= order._id %>">
                                                <button type="submit">Mark as Served</button>
                                            </form>
                                            <% } %>
                                    </div>
                                </div>
                                
                            </div>
        
                        </div>
        
        
                        <% }) %>
        
                </div>
            </div>
        </div>
    </main>


</body>

</html>